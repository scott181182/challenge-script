name: Test 1
command: "echo {{part}}"
parts:
  group1:
    cases:
      test:
        expected:
          stdout: group1
  group2:
    command: ["echo", "{{part}}", "and", "{{case}}"]
    cases:
      test:
        expected:
          stdout: group2 and test
  group3:
    command:
      script: "echo {{part}} and {{case}}"
      template: false
    cases:
      test:
        expected:
          stdout: "{{part}} and {{case}}"
